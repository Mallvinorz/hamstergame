using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GerakHamster : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject hamster;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
